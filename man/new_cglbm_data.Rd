% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cglbm_core.R
\name{new_cglbm_data}
\alias{new_cglbm_data}
\alias{exact_cglbm_data}
\alias{apply_detection_limits}
\alias{print.cglbm_data}
\title{Construct reported CGLBM data}
\usage{
new_cglbm_data(
  value,
  status = NULL,
  lower = NULL,
  upper = NULL,
  latent_truth = NULL,
  metadata = list()
)

exact_cglbm_data(y, metadata = list())

apply_detection_limits(
  y,
  lower_limit = -Inf,
  upper_limit = Inf,
  metadata = list()
)

\method{print}{cglbm_data}(x, ...)
}
\arguments{
\item{value}{Numeric matrix of exact values. Non-exact and missing cells
may be \code{NA}.}

\item{status}{Optional character matrix of report types.}

\item{lower, upper}{Optional numeric matrices of report bounds.}

\item{latent_truth}{Optional latent matrix retained only for simulation
diagnostics; it is never used in fitting.}

\item{metadata}{Optional list of application metadata. A fixed reporting
rule may be supplied as \code{metadata$reporting_design$lower_limit} and
\code{metadata$reporting_design$upper_limit}.}

\item{y}{Numeric matrix of latent or observed values; \code{NA} cells are
recorded as missing.}

\item{lower_limit, upper_limit}{Scalar, column-specific vector, or matrix of
fixed detection limits.}

\item{x}{A \code{cglbm_data} object.}

\item{...}{Additional arguments, currently unused.}
}
\value{
An object of class \code{cglbm_data}.
}
\description{
Create the matrix-valued data representation used by \code{\link[=censored_lbm]{censored_lbm()}}.
Each cell is recorded as \code{"exact"}, \code{"left"}, \code{"right"}, \code{"interval"},
or \code{"missing"}, with report bounds on the same analysis scale as the
values. Missing cells are represented by the whole real line.
}
\examples{
x <- matrix(c(-2, -0.5, 0.5, 2), nrow = 2)
dat <- apply_detection_limits(x, lower_limit = -1, upper_limit = 1)
dat
}
