% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selection.R
\name{cglbm_icl_bic}
\alias{cglbm_icl_bic}
\alias{select_cglbm_model}
\alias{print.cglbm_model_selection}
\title{Select row and column class numbers by ICL}
\usage{
cglbm_icl_bic(fit, data)

select_cglbm_model(
  data,
  K = 1:5,
  L = 1:5,
  algorithm = "SS",
  n_starts = 20L,
  seed = 1L,
  max_iter = 400L,
  tol = 1e-07,
  sigma2_min = 1e-08,
  effective_weight_min = 1e-08,
  monotone_tolerance = 1e-07,
  max_start_attempts = 200L,
  verbose = FALSE,
  S = "Skl",
  P = "free"
)

\method{print}{cglbm_model_selection}(x, ...)
}
\arguments{
\item{fit}{A fitted \code{cglbm_fit} object.}

\item{data}{A \code{cglbm_data} object or numeric matrix.}

\item{K, L}{Candidate row and column class numbers.}

\item{algorithm, S, P}{Fixed model structures passed to \code{\link[=censored_lbm]{censored_lbm()}}.}

\item{n_starts}{Number of valid starts requested for each candidate.}

\item{seed}{Base integer seed used to construct deterministic candidate seeds.}

\item{max_iter}{Maximum GEM iterations per start.}

\item{tol}{Relative objective convergence tolerance.}

\item{sigma2_min}{Positive variance floor.}

\item{effective_weight_min}{Minimum admissible row, column, and observed
block mass.}

\item{monotone_tolerance}{Allowed numerical decrease in a retained GEM
substep.}

\item{max_start_attempts}{Maximum attempted starts for each candidate.}

\item{verbose}{Emit per-candidate progress messages.}

\item{x}{A \code{cglbm_model_selection} object.}

\item{...}{Additional arguments, currently unused.}
}
\value{
\code{cglbm_icl_bic()} returns one numeric ICL score.

A \code{cglbm_model_selection} object containing the candidate table,
retained fits, and selected fit.
}
\description{
Fit a grid of candidate class-number pairs under one fixed membership,
variance, and class-proportion structure, then maximize the implemented
BIC approximation to the integrated classification likelihood.
}
\examples{
y <- matrix(seq_len(24), nrow = 6)
sel <- select_cglbm_model(
  exact_cglbm_data(y), K = 1, L = 1,
  n_starts = 1, max_start_attempts = 1, max_iter = 10, seed = 1
)
sel
}
