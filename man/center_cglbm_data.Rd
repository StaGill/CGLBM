% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{center_cglbm_data}
\alias{center_cglbm_data}
\title{Center reported matrix columns under censoring}
\usage{
center_cglbm_data(
  data,
  variance = c("pooled", "column"),
  max_iter = 5000L,
  tolerance = 1e-11,
  score_tolerance = 1e-06,
  parameter_tolerance = 1e-07
)
}
\arguments{
\item{data}{A \code{cglbm_data} object or numeric matrix interpreted as exact
data.}

\item{variance}{\code{"pooled"} for one residual variance or \code{"column"} for one
variance per column.}

\item{max_iter}{Maximum EM iterations.}

\item{tolerance}{Relative log-likelihood convergence tolerance.}

\item{score_tolerance}{Maximum absolute score used in convergence checks.}

\item{parameter_tolerance}{Maximum relative parameter change used in
convergence checks.}
}
\value{
A centered \code{cglbm_data} object. Estimation details are stored in
\code{metadata$centering}.
}
\description{
Estimate one center per column from exact and one-sided censored reports,
using either a residual variance pooled across columns or one variance per
column. The function subtracts centers from exact values, report bounds,
latent simulation values, and fixed reporting-design limits; it never
divides by a scale.
}
\examples{
x <- matrix(c(1, 2, 3, 4, 10, 11, 12, 13), nrow = 4)
centered <- center_cglbm_data(exact_cglbm_data(x), variance = "pooled")
centered$metadata$centering$centers
}
