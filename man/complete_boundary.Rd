% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completion_methods.R
\name{complete_boundary}
\alias{complete_boundary}
\alias{complete_halfdouble}
\alias{complete_truncnorm}
\alias{complete_mi}
\alias{pool_mi_fits}
\title{Complete censored reports before co-clustering}
\usage{
complete_boundary(data)

complete_halfdouble(data, analysis_scale = c("log", "log2"))

complete_truncnorm(data, seed)

complete_mi(data, seed, m = 10L, burnin = 50L, thin = 20L)

pool_mi_fits(fits)
}
\arguments{
\item{data}{A \code{cglbm_data} object.}

\item{analysis_scale}{Scale used for half/double substitution.}

\item{seed}{Integer random seed.}

\item{m}{Number of multiple-imputation matrices.}

\item{burnin}{Number of initial sampler iterations discarded.}

\item{thin}{Iterations between retained imputations.}

\item{fits}{List of fitted CGLBM objects from multiple imputations.}
}
\value{
\code{complete_boundary()}, \code{complete_halfdouble()}, and
\code{complete_truncnorm()} return an exact \code{cglbm_data} object.
\code{complete_mi()} returns a list with \code{completed} numeric matrices,
parameter \code{draws}, and sampler \code{diagnostics}. \code{pool_mi_fits()} returns a
pooled \code{cglbm_fit} after fitting the completed matrices separately.
}
\description{
Completion comparators used in the accompanying manuscript. Boundary and
half/double substitution are deterministic. Truncated-normal and multiple
imputation require explicit seeds. Completed matrices are returned as exact
\code{cglbm_data} objects.
}
\examples{
x <- matrix(c(-2, -0.5, 0.5, 2, -1.5, 0, 1, 2.5), nrow = 4)
dat <- apply_detection_limits(x, lower_limit = -1, upper_limit = 2)
complete_boundary(dat)
complete_halfdouble(dat, analysis_scale = "log2")
}
