% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cglbm_core.R
\name{censored_lbm}
\alias{censored_lbm}
\alias{print.cglbm_fit}
\alias{summary.cglbm_fit}
\alias{predict.cglbm_fit}
\title{Fit a censored Gaussian latent block model}
\usage{
censored_lbm(
  data,
  g,
  m,
  algorithm = "SS",
  S = c("Skl", "Sk", "Sl", "S0"),
  P = c("free", "equal"),
  n_starts = 20L,
  starts = NULL,
  seed = 1L,
  max_iter = 400L,
  tol = 1e-07,
  sigma2_min = 1e-08,
  effective_weight_min = 1e-08,
  monotone_tolerance = 1e-07,
  max_start_attempts = NULL,
  require_map_nonempty = TRUE,
  verbose = FALSE
)

\method{print}{cglbm_fit}(x, ...)

\method{summary}{cglbm_fit}(object, ...)

\method{predict}{cglbm_fit}(object, type = c("labels", "memberships", "parameters"), ...)
}
\arguments{
\item{data}{A \code{\link[=new_cglbm_data]{new_cglbm_data()}} object or a numeric matrix interpreted as
exact data.}

\item{g, m}{Positive numbers of row and column classes.}

\item{algorithm}{Membership configuration: \code{"HH"}, \code{"HS"}, \code{"SH"}, or
\code{"SS"}; the first letter refers to rows and the second to columns.}

\item{S}{Variance structure: block-specific (\code{"Skl"}), shared by row class
(\code{"Sk"}), shared by column class (\code{"Sl"}), or globally shared (\code{"S0"}).}

\item{P}{Class-proportion structure: estimated freely (\code{"free"}) or fixed
equally (\code{"equal"}).}

\item{n_starts}{Number of valid starts requested.}

\item{starts}{Optional ordered list of starts, each containing integer
vectors \code{z} and \code{w}.}

\item{seed}{Integer random seed.}

\item{max_iter}{Maximum GEM iterations per start.}

\item{tol}{Relative objective convergence tolerance.}

\item{sigma2_min}{Positive variance floor.}

\item{effective_weight_min}{Minimum admissible row, column, and observed
block mass.}

\item{monotone_tolerance}{Allowed numerical decrease in a retained GEM
substep.}

\item{max_start_attempts}{Maximum attempted starts.}

\item{require_map_nonempty}{Require every declared class in the final MAP
partitions.}

\item{verbose}{Emit per-attempt progress messages.}

\item{x}{A \code{cglbm_fit} object.}

\item{...}{Additional arguments, currently unused.}

\item{object}{A \code{cglbm_fit} object.}

\item{type}{One of \code{"labels"}, \code{"memberships"}, or \code{"parameters"}.}
}
\value{
A \code{cglbm_fit} object containing memberships, MAP labels, fitted
parameters, objective diagnostics, and Fisher-information summaries.

\code{summary.cglbm_fit()} returns a data frame with one row per block;
\code{predict.cglbm_fit()} returns labels, memberships, or parameters.
}
\description{
Jointly estimate row memberships, column memberships, class proportions,
and Gaussian block parameters from exact, censored, and missing reports.
The five profiled numerical kernels use the compiled Rcpp backend; starts,
validity rules, convergence, and model control remain in R.
}
\examples{
y <- matrix(c(-2.1, -1.9, 1.9, 2.1,
              -2.0, -1.8, 1.8, 2.0), nrow = 4)
dat <- exact_cglbm_data(y)
fit <- censored_lbm(
  dat, g = 2, m = 1, algorithm = "HH",
  n_starts = 1,
  starts = list(list(z = c(1L, 1L, 2L, 2L), w = rep(1L, 2))),
  max_iter = 30, max_start_attempts = 1, seed = 1
)
predict(fit, type = "labels")
summary(fit)
}
